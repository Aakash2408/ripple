{
  "id": "typescript-openapi-remove-field",
  "cell": {
    "language": "typescript",
    "contract": "openapi",
    "operation": "remove_field"
  },
  "change": {
    "spec_before": "spec/user.before.yaml",
    "spec_after": "spec/user.after.yaml",
    "field": "phoneNumber",
    "category": "mechanical"
  },
  "consumer": {
    "root": "consumer",
    "install": [
      "npm",
      "install",
      "--ignore-scripts",
      "--no-audit",
      "--no-fund"
    ],
    "typecheck": [
      "./node_modules/.bin/tsc",
      "--noEmit"
    ],
    "node": ">=14.17"
  },
  "expect": {
    "consumers_found": [
      "src/checkout.ts"
    ],
    "untouched": [
      "src/types.ts",
      "src/orders.ts",
      "tsconfig.json",
      "package.json"
    ],
    "transformation": [
      {
        "site": "formatContact",
        "shape": "template-literal placeholder",
        "action": "drop `${user.phoneNumber}` from the interpolation",
        "mechanical": true,
        "why": "a display string; removing it cannot change behaviour"
      },
      {
        "site": "toCrmPayload",
        "shape": "object-literal value",
        "action": "drop the `phone: user.phoneNumber,` entry",
        "mechanical": true,
        "why": "the field no longer exists upstream, so sending it is meaningless"
      }
    ],
    "typecheck_before_fix": "FAIL",
    "typecheck_before_fix_errors": 2,
    "typecheck_after_fix": "PASS",
    "terminal_state": "RESOLVED",
    "pr_files": [
      "src/checkout.ts"
    ]
  },
  "measured": {
    "note": "Re-measured in Stage 6 after app/ts_codemod.py replaced the regex. The Stage 4 record is preserved below as `stage_4_baseline` -- it is why the codemod exists.",
    "as_of": "2026-08-18",
    "fixture_fails_without_a_fix": true,
    "fixture_is_satisfiable": true,
    "ripple_changed_the_file": true,
    "ripple_output_parses": true,
    "ripple_typecheck_after": "VALID",
    "ripple_edits": [
      "template interpolation: ${user.phoneNumber}",
      "object-literal property: phone: user.phoneNumber,"
    ],
    "ripple_diff_minimal": true,
    "untouched_verified_by": "byte comparison of src/orders.ts, src/types.ts, tsconfig.json, package.json",
    "validation_backend": "docker, node:16-alpine, --network none for typecheck",
    "evidence_test": "test_e2e_typescript_openapi_remove_field",
    "production_ready": true,
    "stage_4_baseline": {
      "ripple_changed_the_file": true,
      "ripple_output_parses": false,
      "ripple_diff": "-    phone: user.phoneNumber,\n-  };\n+    phone: user.};",
      "ripple_typecheck_error": "src/checkout.ts(19,17): error TS1003: Identifier expected",
      "ripple_explanation": "Removed all references to field 'phoneNumber' (1 lines affected).",
      "verdict": "BROKEN FIX -- the reason Stage 6 wrote a syntax-aware codemod"
    }
  }
}
